defmodule MoBankWeb.AccountControllerTest do
  use MoBankWeb.ConnCase

  alias MoBankWeb.AccountController

  describe "create an account" do
    test "it should create an account successfully" do
    end

    test "it should return an error when parameters is missing" do
    end

    test "it should return an error when parameters type is incorrect" do
    end
  end
end
