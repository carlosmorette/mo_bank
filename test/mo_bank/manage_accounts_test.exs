defmodule MoBank.ManageAccountsTest do
  use ExUnit.Case

  alias MoBank.ManageAccounts

  describe "create/1" do
    test "it should create an account and return his fields" do
    end

    test "it should return an error when is missing parameters" do
    end

    test "it should return an error when parameters type is incorrect" do
    end
  end
end
